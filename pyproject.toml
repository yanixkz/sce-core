[project]
name = "sce-core"
version = "0.1.0-alpha"
description = "State–Constraint–Evolution Core"
authors = [{ name = "SCE Authors" }]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
sce = "sce.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
